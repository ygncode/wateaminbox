---
import Layout from "../../layouts/Layout.astro";

const sections = [
  { id: "getting-started", label: "Orientation" },
  { id: "installation", label: "Local setup" },
  { id: "configuration", label: "Configuration" },
  { id: "connecting-whatsapp", label: "WhatsApp connections" },
  { id: "team-management", label: "Teams and access" },
  { id: "features", label: "Implemented features" },
  { id: "production", label: "Production self-hosting" },
  { id: "api-reference", label: "API guide" },
  { id: "faq", label: "FAQ" },
];

const services = [
  ["Web app", "4444", "React inbox and workspace UI"],
  ["API", "4445", "Hono API on Bun"],
  ["Marketing", "4446", "Static Astro site"],
  ["PostgreSQL", "4447", "Application and tenant data"],
  ["NATS", "4448", "Commands, events, and JetStream"],
  ["Meilisearch", "4449", "Search index; PostgreSQL is the fallback"],
  ["MinIO", "4450 / 9001", "S3-compatible media / local console"],
  ["Centrifugo", "4451", "Authenticated realtime WebSocket gateway"],
];

const features = [
  {
    number: "01",
    title: "Shared conversations",
    description:
      "Connect multiple WhatsApp accounts to one workspace. The inbox keeps messages, delivery state, reactions, media, presence, typing indicators, groups, and connection-specific history together.",
  },
  {
    number: "02",
    title: "Ownership and context",
    description:
      "Assign contacts, restrict member visibility to assigned conversations when appropriate, and keep shared or private notes, tags, and assignment history with the contact.",
  },
  {
    number: "03",
    title: "Business inbox tools",
    description:
      "Use quick replies, sync WhatsApp Business labels to workspace tags, review synced catalogs and products, and control product visibility from the workspace settings.",
  },
  {
    number: "04",
    title: "Search and data tools",
    description:
      "Search contacts and messages, import contacts from CSV, export contacts or messages as CSV/JSON, and create a full ZIP backup when the caller has the required access.",
  },
  {
    number: "05",
    title: "Operational views",
    description:
      "The dashboard includes message, contact, team, engagement, response-time, and resolution views. Conversation cases and calendar-aware SLA policies support the operational workflow.",
  },
  {
    number: "06",
    title: "Durable background work",
    description:
      "NATS JetStream, transactional outboxes, worker event handling, and idempotent processing cover the command and synchronization paths. Realtime events are update signals; PostgreSQL remains the source of truth.",
  },
  {
    number: "07",
    title: "Notifications",
    description:
      "Use the in-app notification center and browser desktop notifications. Web Push is optional and requires a stable VAPID key pair plus HTTPS outside localhost.",
  },
  {
    number: "08",
    title: "Scheduled and bulk messages",
    description:
      "The application includes scheduled messages and permission-gated broadcast jobs. Bulk sending is paced and quota-limited by configuration; operators remain responsible for consent, policy, and WhatsApp account risk.",
  },
];

const apiExamples = [
  ["GET", "/api/health/live", "Liveness probe; no workspace context"],
  ["POST", "/api/auth/register", "Create a user and send verification mail"],
  ["GET", "/api/contacts", "List visible contacts with filters"],
  ["POST", "/api/messages", "Canonical message-send endpoint"],
  ["GET", "/api/conversations/:id/messages", "Read a conversation page"],
  ["GET", "/api/search?q=...", "Search visible contacts and messages"],
  ["GET", "/api/analytics/dashboard", "Workspace dashboard data"],
  ["POST", "/api/realtime/token", "Issue a short-lived Centrifugo token"],
  ["GET", "/api/export/full", "Create a permission-gated ZIP backup"],
];

const permissionRows = [
  ["Owner", "All permissions; ownership cannot be removed by the permission editor."],
  ["Admin", "All role defaults, subject to the owner/admin hierarchy rules for team operations."],
  ["Member", "Can send messages and normally sees assigned conversations only."],
];
---

<Layout
  title="Self-hosting documentation — WATeamInbox"
  description="Current documentation for understanding WATeamInbox, running the open-source beta locally, and operating its self-hosted stack."
  imageAlt="WATeamInbox self-hosting documentation"
>
  <div class="docs-page">
    <section class="docs-hero">
      <div class="docs-hero-copy">
        <p class="docs-kicker"><span></span> Public docs / self-hosting</p>
        <h1>Run the inbox where your team can see the boundaries.</h1>
        <p class="docs-hero-lede">
          WATeamInbox is an open-source beta for teams sharing customer
          conversations on WhatsApp. This guide describes the code that is in
          the repository now: local setup, configuration, connections, access,
          and the self-hosted production baseline.
        </p>
        <div class="docs-hero-actions">
          <a href="#installation" class="docs-button">Start locally <span aria-hidden="true">↓</span></a>
          <a href="https://github.com/ygncode/wateaminbox" target="_blank" rel="noreferrer" class="docs-text-link">Open the repository <span aria-hidden="true">↗</span></a>
        </div>
      </div>
      <aside class="docs-hero-card" aria-label="Documentation scope">
        <p class="docs-card-label">Read this first</p>
        <h2>Self-hosting is the available path.</h2>
        <p>
          The repository is MIT-licensed and can be operated on infrastructure
          you control. WATeamInbox Cloud is only a planned option; the waitlist
          does not create a product account or grant access.
        </p>
        <div class="docs-card-rule"></div>
        <dl>
          <div><dt>Runtime</dt><dd>Bun + Go</dd></div>
          <div><dt>Data</dt><dd>PostgreSQL + S3-compatible storage</dd></div>
          <div><dt>Realtime</dt><dd>NATS + Centrifugo</dd></div>
        </dl>
      </aside>
    </section>

    <div class="docs-layout">
      <aside class="docs-toc">
        <p class="docs-toc-label">On this page</p>
        <nav aria-label="Documentation sections">
          <ol>
            {sections.map((section, index) => (
              <li>
                <a href={`#${section.id}`}><span>{String(index + 1).padStart(2, "0")}</span>{section.label}</a>
              </li>
            ))}
          </ol>
        </nav>
        <div class="docs-toc-note">
          <strong>Source of truth</strong>
          <p>Keep the root <code>README.md</code>, <code>.env.example</code>, and the linked deployment guide nearby when operating the stack.</p>
        </div>
      </aside>

      <div class="docs-content">
        <section id="getting-started" class="docs-section docs-intro">
          <p class="docs-section-number">01 / orientation</p>
          <h2>Know what you are running.</h2>
          <p class="docs-lede">
            WATeamInbox is a multi-tenant collaborative inbox. A workspace can
            connect WhatsApp devices, bring messages into a shared React UI, and
            give each teammate a controlled view of the work. Its WhatsApp
            integration is unofficial and the software is still a community
            beta.
          </p>
          <div class="docs-truth-grid">
            <article>
              <p class="docs-card-label">Available now</p>
              <h3>Self-host the open-source beta</h3>
              <p>
                Run the web app, Bun API, Go workers, PostgreSQL, NATS,
                Centrifugo, search, and media storage on infrastructure you
                operate.
              </p>
              <a href="#production">Read the production notes <span aria-hidden="true">↗</span></a>
            </article>
            <article class="docs-truth-planned">
              <p class="docs-card-label">Planned, not live</p>
              <h3>WATeamInbox Cloud</h3>
              <p>
                The public waitlist collects confirmed interest for a future
                managed option. There is no Cloud account, price, launch date,
                feature set, or service level promised here.
              </p>
              <a href="/#waitlist">See the waitlist <span aria-hidden="true">↗</span></a>
            </article>
          </div>
          <div class="docs-callout docs-callout-warning">
            <strong>Beta and account-risk notice.</strong>
            <p>
              Review the integration, data handling, backups, monitoring,
              updates, and WhatsApp terms before using this software with real
              customer data. No deployment or project page can guarantee that a
              WhatsApp account will avoid restrictions.
            </p>
          </div>
        </section>

        <section id="installation" class="docs-section">
          <p class="docs-section-number">02 / local setup</p>
          <h2>Get a complete development stack running.</h2>
          <p>
            The root development compose file provides PostgreSQL, NATS,
            Centrifugo, Meilisearch, and MinIO. The root <code>bun run dev</code>
            command starts the application processes, including the web and
            marketing servers.
          </p>

          <div class="docs-two-column docs-prerequisites">
            <div>
              <h3>Prerequisites</h3>
              <ul class="docs-check-list">
                <li><strong>Bun 1.2.18+</strong><span>Workspace package manager and TypeScript runtime.</span></li>
                <li><strong>Go 1.25+</strong><span>Builds the orchestrator and WhatsApp worker.</span></li>
                <li><strong>Docker Compose</strong><span>Runs local data and messaging dependencies.</span></li>
                <li><strong>Git</strong><span>Clones the repository and keeps source changes reviewable.</span></li>
              </ul>
            </div>
            <div class="docs-side-note">
              <p class="docs-card-label">Before you begin</p>
              <p>
                Local compose uses development credentials and exposed ports.
                It is for development only, not a production deployment.
              </p>
              <a href="https://github.com/ygncode/wateaminbox/blob/main/README.md" target="_blank" rel="noreferrer">Read the root README <span aria-hidden="true">↗</span></a>
            </div>
          </div>

          <h3 class="docs-subheading">Quick start</h3>
          <pre class="docs-code"><code>git clone https://github.com/ygncode/wateaminbox.git
cd wateaminbox

# Create the ignored local environment file.
cp .env.example .env

# Install JavaScript dependencies and fetch Go dependencies during builds.
bun install

# Start PostgreSQL, NATS, Centrifugo, Meilisearch, and MinIO.
docker compose up -d

# Create the central and tenant database structures.
bun run db:migrate

# Start the web app, API, workers, and marketing site.
bun run dev</code></pre>
          <p class="docs-caption">
            Open the web app at <code>http://localhost:4444</code>. The API is at
            <code>http://localhost:4445/api</code>; the marketing site uses
            <code>http://localhost:4446</code>.
          </p>

          <h3 class="docs-subheading">Local service map</h3>
          <div class="docs-table-wrap">
            <table class="docs-table">
              <thead><tr><th>Service</th><th>Port</th><th>Purpose</th></tr></thead>
              <tbody>
                {services.map(([name, port, purpose]) => (
                  <tr><td>{name}</td><td><code>{port}</code></td><td>{purpose}</td></tr>
                ))}
              </tbody>
            </table>
          </div>
          <p class="docs-caption">
            NATS monitoring is available at <code>http://localhost:8222</code>.
            The optional NATS toolbox can be started with
            <code>docker compose --profile debug up -d nats-box</code>.
          </p>
        </section>

        <section id="configuration" class="docs-section">
          <p class="docs-section-number">03 / configuration</p>
          <h2>Configure the runtime without guessing.</h2>
          <p>
            The checked-in root <code>.env.example</code> is the authoritative
            development inventory. Copy it, then change the secrets and any
            hostnames that differ from the defaults. Environment variables are
            loaded from the repository root when commands run from the root.
          </p>

          <div class="docs-callout docs-callout-warning">
            <strong>Replace development secrets.</strong>
            <p>
              At minimum, set independent random values for
              <code>JWT_SECRET</code>, <code>CENTRIFUGO_API_KEY</code>, and
              <code>CENTRIFUGO_TOKEN_HMAC_SECRET</code>. The HMAC secret and JWT
              secret must not be the same. Never commit <code>.env</code>, VAPID
              private keys, mail keys, WhatsApp session data, or storage credentials.
            </p>
          </div>

          <div class="docs-config-grid">
            <article>
              <p class="docs-card-label">Application and data</p>
              <ul>
                <li><code>DATABASE_URL</code> — PostgreSQL connection.</li>
                <li><code>NATS_URL</code> — NATS server used by the API and workers.</li>
                <li><code>S3_*</code> — MinIO locally or private S3/R2-compatible storage.</li>
                <li><code>MEILISEARCH_*</code> — search service; PostgreSQL search is used when Meilisearch is unavailable.</li>
              </ul>
            </article>
            <article>
              <p class="docs-card-label">Auth and realtime</p>
              <ul>
                <li><code>JWT_SECRET</code> — access and refresh token signing.</li>
                <li><code>CENTRIFUGO_*</code> — server API, health, and token-signing configuration.</li>
                <li><code>APP_URL</code> and <code>CORS_ORIGINS</code> — browser origin policy.</li>
                <li><code>RATE_LIMIT_*</code> — request and resource limits; production cannot disable rate limiting.</li>
              </ul>
            </article>
            <article>
              <p class="docs-card-label">Email and notifications</p>
              <ul>
                <li><code>MAIL_DRIVER=log</code> captures local mail without delivery.</li>
                <li>Production requires the Resend driver, a real <code>EMAIL_FROM</code>, and a real provider key.</li>
                <li>Web Push is optional: configure <code>VAPID_PUBLIC_KEY</code>, <code>VAPID_PRIVATE_KEY</code>, and <code>VAPID_SUBJECT</code>.</li>
              </ul>
            </article>
          </div>

          <h3 class="docs-subheading">Browser URLs</h3>
          <p>
            The web client defaults to the local API URL. Set these values when
            the browser reaches the API or Centrifugo through a different host,
            port, or TLS endpoint:
          </p>
          <pre class="docs-code"><code>VITE_API_URL=http://localhost:4445/api
VITE_CENTRIFUGO_URL=ws://localhost:4451/connection/websocket</code></pre>

          <h3 class="docs-subheading">The static marketing site and waitlist</h3>
          <p>
            The marketing site is a static Astro build. Its Cloud waitlist form
            uses the explicit build-time <code>PUBLIC_WAITLIST_API_URL</code>;
            it does not guess a missing <code>/api</code> proxy. If this copy is
            self-hosted without a waitlist Worker, leave the value unset and the
            form explains that no signup service is configured.
          </p>
          <pre class="docs-code"><code># Separate Worker origin for a self-hosted marketing deployment.
PUBLIC_WAITLIST_API_URL=https://waitlist-api.example.com

# Optional public Turnstile site key; the Worker validates the token server-side.
PUBLIC_WAITLIST_TURNSTILE_SITE_KEY=your-public-site-key</code></pre>
          <p class="docs-caption">
            The documented Cloudflare Pages setup uses the same-origin Worker
            route at <code>https://wateaminbox.com</code>. Use that value only
            when the reviewed route exists; otherwise use the real Worker origin.
            See the <a href="https://github.com/ygncode/wateaminbox/blob/main/docs/cloudflare-waitlist.md" target="_blank" rel="noreferrer">Cloud waitlist guide <span aria-hidden="true">↗</span></a>.
          </p>
        </section>

        <section id="connecting-whatsapp" class="docs-section">
          <p class="docs-section-number">04 / WhatsApp connections</p>
          <h2>Pair a device, then let the sync settle.</h2>
          <ol class="docs-steps">
            <li><span>1</span><div><strong>Create and verify your user.</strong><p>Open the web app at your configured <code>APP_URL</code>, register, and verify the email link. Create a workspace or accept an invitation.</p></div></li>
            <li><span>2</span><div><strong>Open Settings → Connections.</strong><p>Add a named WhatsApp connection. The default maximum is five active or pending connections; a company-level setting can change that limit.</p></div></li>
            <li><span>3</span><div><strong>Scan the pairing QR code.</strong><p>On the phone, open WhatsApp → Settings → Linked Devices → Link a Device, then scan the code shown by the workspace. The code expires quickly; restart the pairing attempt if the UI reports expiry.</p></div></li>
            <li><span>4</span><div><strong>Watch the initial history sync.</strong><p>Messages, contacts, groups, receipts, and available media are imported through the worker and API. The UI exposes sync progress; a connected account may continue processing history after pairing.</p></div></li>
          </ol>
          <div class="docs-two-column docs-connection-notes">
            <div class="docs-callout">
              <strong>Connection lifecycle</strong>
              <p>
                Disconnect stops the worker while retaining the linked-device
                session for reconnect. Archive &amp; unlink logs out and removes
                session credentials while retaining inbox history. Permanent
                deletion is separate, permission-gated, and only applies to an
                archived connection.
              </p>
            </div>
            <div class="docs-callout">
              <strong>If history is missing</strong>
              <p>
                Let the first sync finish, check the connection health, and use
                the conversation's older-history action when the local page is
                exhausted. Older history depends on what the primary WhatsApp
                device makes available.
              </p>
            </div>
          </div>
        </section>

        <section id="team-management" class="docs-section">
          <p class="docs-section-number">05 / teams and access</p>
          <h2>Make visibility and ownership explicit.</h2>
          <p>
            Access is workspace-scoped. Roles provide defaults, while the owner
            can apply custom permissions to admins and members. Conversation
            visibility is not automatically global: without
            <code>can_view_all_chats</code>, a member normally sees active
            assignments.
          </p>
          <div class="docs-role-list">
            {permissionRows.map(([role, description]) => (
              <article><h3>{role}</h3><p>{description}</p></article>
            ))}
          </div>
          <div class="docs-two-column docs-team-grid">
            <div>
              <h3>Invite and hand off</h3>
              <ul class="docs-bullet-list">
                <li>Owners and members with <code>can_invite</code> can invite an admin or member.</li>
                <li>Invitation links expire after seven days and can be resent or canceled.</li>
                <li>An unassigned contact can be claimed; reassignment requires the relevant assignment permission.</li>
                <li>A first send can claim an unassigned contact, while an active assignment remains an access boundary.</li>
              </ul>
            </div>
            <div>
              <h3>Permission areas</h3>
              <ul class="docs-bullet-list">
                <li>Chat visibility, sending, and broadcast sending.</li>
                <li>Contact assignment, team invites, and team administration.</li>
                <li>WhatsApp connection management, dashboard, and audit log access.</li>
                <li>Exports and destructive data actions.</li>
              </ul>
            </div>
          </div>
        </section>

        <section id="features" class="docs-section">
          <p class="docs-section-number">06 / implemented features</p>
          <h2>A capability map, not a roadmap.</h2>
          <p>
            These are the main surfaces represented by the current web, API,
            database, and worker code. Feature access still depends on role,
            connection state, migrations, and configuration.
          </p>
          <div class="docs-feature-grid">
            {features.map((feature) => (
              <article>
                <span class="docs-feature-number">{feature.number}</span>
                <h3>{feature.title}</h3>
                <p>{feature.description}</p>
              </article>
            ))}
          </div>
          <div class="docs-callout docs-callout-note">
            <strong>Labels are a sync workflow, not magic bidirectional state.</strong>
            <p>
              A connection manager can request a label sync, link labels to
              workspace tags, auto-create tags, and apply or remove a label from
              a contact. Treat the WhatsApp account and the workspace as systems
              that can diverge until a sync or action completes.
            </p>
          </div>
        </section>

        <section id="production" class="docs-section">
          <p class="docs-section-number">07 / production self-hosting</p>
          <h2>Use the hardened baseline, then do your own review.</h2>
          <p>
            Do not promote the development <code>docker-compose.yml</code> to
            production. The repository's <code>compose.production.yml</code>
            is a single-host baseline with Caddy at the edge and private data
            services. It is deployment guidance, not a hosted service or a
            guarantee of production readiness for every environment.
          </p>
          <div class="docs-production-grid">
            <article><span>01</span><h3>Secrets and TLS</h3><p>Use <code>.env.production.example</code> as a template, generate separate secret files, keep ingress on HTTPS, and do not reuse development credentials.</p></article>
            <article><span>02</span><h3>Data and media</h3><p>Keep PostgreSQL authoritative, use private S3-compatible media storage, and define encrypted backups for database, media, NATS, search, and secret material.</p></article>
            <article><span>03</span><h3>Operations</h3><p>Run migrations as a gated step, pin image tags, monitor health and queue signals, test restores, and document upgrade and rollback decisions.</p></article>
          </div>
          <div class="docs-callout docs-callout-note">
            <strong>Read the full deployment procedure before exposing a host.</strong>
            <p>
              It covers DNS, Caddy, secret files, the migration service, backups,
              restore drills, proxy trust, monitoring, upgrades, and rollback.
              <a href="https://github.com/ygncode/wateaminbox/blob/main/docs/deployment.md" target="_blank" rel="noreferrer">Open docs/deployment.md <span aria-hidden="true">↗</span></a>
            </p>
          </div>
        </section>

        <section id="api-reference" class="docs-section">
          <p class="docs-section-number">08 / API guide</p>
          <h2>Integrate against the routes that exist.</h2>
          <p>
            The API base is <code>/api</code> in the documented reverse-proxy
            shape, or <code>http://localhost:4445/api</code> locally. Most
            workspace routes require a short-lived bearer access token and the
            <code>X-Company-ID</code> header. Refresh tokens are held in an
            HttpOnly cookie; do not copy server secrets into browser variables.
          </p>
          <pre class="docs-code"><code>Authorization: Bearer &lt;access-token&gt;
X-Company-ID: &lt;workspace-uuid&gt;</code></pre>
          <div class="docs-table-wrap">
            <table class="docs-table docs-api-table">
              <thead><tr><th>Method</th><th>Path</th><th>Use</th></tr></thead>
              <tbody>
                {apiExamples.map(([method, path, use]) => (
                  <tr><td><span class={`docs-method docs-method-${method.toLowerCase()}`}>{method}</span></td><td><code>{path}</code></td><td>{use}</td></tr>
                ))}
              </tbody>
            </table>
          </div>
          <p class="docs-caption">
            The canonical send route is <code>POST /api/messages</code>.
            <code>POST /api/conversations/:id/messages</code> remains for
            compatibility and is deprecated. Export routes return CSV, JSON, or
            ZIP rather than only JSON. See <a href="https://github.com/ygncode/wateaminbox/tree/main/apps/api/src/routes" target="_blank" rel="noreferrer">the route source <span aria-hidden="true">↗</span></a> for the current surface.
          </p>

          <h3 class="docs-subheading">Realtime transport</h3>
          <p>
            The browser obtains a short-lived token from
            <code>POST /api/realtime/token</code> and connects to the configured
            Centrifugo WebSocket endpoint. Events invalidate or update local
            UI state; clients reconcile with PostgreSQL-backed API reads after
            reconnects.
          </p>
          <pre class="docs-code"><code>message:new
message:status
contact:updated
connection:status
notification:new
labels:updated
catalogs:updated</code></pre>
        </section>

        <section id="faq" class="docs-section docs-faq">
          <p class="docs-section-number">09 / FAQ</p>
          <h2>Short answers for the decisions that matter.</h2>
          <div class="docs-faq-list">
            <details open><summary>Is WATeamInbox an official WhatsApp product?</summary><p>No. It is an independent project and is not affiliated with, endorsed by, or sponsored by WhatsApp or Meta.</p></details>
            <details><summary>Is there a hosted Cloud service to use now?</summary><p>No. The Cloud waitlist is for updates about a planned managed option. Joining does not create an account, start billing, or provide access. Self-hosting is the available path in this repository.</p></details>
            <details><summary>Does the project guarantee WhatsApp account safety?</summary><p>No. The integration is unofficial and the project cannot guarantee prevention or reversal of restrictions. Follow WhatsApp's terms and avoid unsolicited or high-volume messaging.</p></details>
            <details><summary>What does the production guide guarantee?</summary><p>Nothing by itself. It documents a concrete single-host baseline. Operators still own domain configuration, secrets, TLS, access control, backups, updates, monitoring, incident response, and compliance decisions.</p></details>
            <details><summary>Can I use Web Push without configuring it?</summary><p>Yes, Web Push is optional. Without it, desktop notifications work while the application is loaded. Configure a stable VAPID pair and HTTPS for background delivery.</p></details>
            <details><summary>Where should I report a bug or security issue?</summary><p>Use <a href="https://github.com/ygncode/wateaminbox/issues" target="_blank" rel="noreferrer">GitHub Issues</a> for reproducible bugs and questions. Report suspected vulnerabilities privately using the repository's <a href="https://github.com/ygncode/wateaminbox/blob/main/SECURITY.md" target="_blank" rel="noreferrer">security policy</a>.</p></details>
          </div>
        </section>

        <section class="docs-feedback">
          <p class="docs-section-number">Keep the guide honest</p>
          <h2>Found a mismatch?</h2>
          <p>
            Documentation should follow the code, configuration, and deployment
            decisions. Send a correction by email or open a focused issue with
            the command, route, or file that needs updating.
          </p>
          <div class="docs-hero-actions">
            <a href="mailto:me@setkyar.com" class="docs-button">Email the project</a>
            <a href="https://github.com/ygncode/wateaminbox/issues" target="_blank" rel="noreferrer" class="docs-text-link">Open an issue <span aria-hidden="true">↗</span></a>
          </div>
        </section>
      </div>
    </div>
  </div>
</Layout>

<style>
  .docs-page {
    --docs-ink: #15211f;
    --docs-muted: #5b6b62;
    --docs-paper: #f8f7f1;
    --docs-paper-dark: #edf1e9;
    --docs-green: #1d633d;
    --docs-green-dark: #14221e;
    --docs-line: #cbd6ca;
    overflow-x: clip;
    background: var(--docs-paper);
    color: var(--docs-ink);
    font-family: var(--font-body);
  }

  .docs-hero {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(20rem, 0.75fr);
    gap: clamp(2rem, 6vw, 6rem);
    width: min(100% - 2rem, 74rem);
    margin: 0 auto;
    padding: clamp(4.5rem, 9vw, 8rem) 0 clamp(4rem, 8vw, 6rem);
  }

  .docs-hero::before {
    position: absolute;
    z-index: 0;
    top: 2rem;
    right: -4rem;
    width: 9rem;
    height: 9rem;
    border: 1px solid #83a487;
    content: "";
    transform: rotate(22deg);
  }

  .docs-hero-copy,
  .docs-hero-card {
    position: relative;
    z-index: 1;
  }

  .docs-kicker,
  .docs-section-number,
  .docs-toc-label,
  .docs-card-label {
    margin: 0;
    color: var(--docs-green);
    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .docs-kicker {
    display: flex;
    align-items: center;
    gap: 0.55rem;
  }

  .docs-kicker span {
    width: 0.55rem;
    height: 0.55rem;
    background: #4d9f62;
  }

  .docs-hero h1,
  .docs-section h2,
  .docs-feedback h2 {
    margin: 1rem 0 0;
    color: var(--docs-ink);
    font-family: var(--font-display);
    font-size: clamp(3rem, 6vw, 5.7rem);
    font-weight: 500;
    letter-spacing: -0.055em;
    line-height: 0.91;
    text-wrap: balance;
  }

  .docs-hero-lede {
    max-width: 43rem;
    margin: 1.8rem 0 0;
    color: var(--docs-muted);
    font-size: clamp(1.05rem, 1.5vw, 1.23rem);
    line-height: 1.65;
    text-wrap: pretty;
  }

  .docs-hero-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 1rem 1.5rem;
    margin-top: 2rem;
  }

  .docs-button,
  .docs-text-link,
  .docs-card-link,
  .docs-truth-grid a,
  .docs-callout a,
  .docs-side-note a {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 700;
    text-decoration: none;
  }

  .docs-button {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    background: var(--docs-green);
    color: #fff;
    padding: 0.9rem 1rem;
  }

  .docs-button:hover {
    background: #12452a;
  }

  .docs-text-link {
    border-bottom: 1px solid #718779;
    color: var(--docs-ink);
    padding-bottom: 0.18rem;
  }

  .docs-text-link:hover,
  .docs-truth-grid a:hover,
  .docs-callout a:hover,
  .docs-side-note a:hover {
    color: var(--docs-green);
  }

  .docs-button:focus-visible,
  .docs-text-link:focus-visible,
  .docs-toc a:focus-visible,
  .docs-truth-grid a:focus-visible,
  .docs-callout a:focus-visible,
  .docs-side-note a:focus-visible,
  .docs-faq summary:focus-visible {
    outline: 3px solid #9bcba1;
    outline-offset: 3px;
  }

  .docs-hero-card {
    align-self: end;
    border: 1px solid #5e7967;
    background: var(--docs-green-dark);
    box-shadow: 12px 12px 0 #d0dfcd;
    color: #e8f1e6;
    padding: clamp(1.5rem, 3vw, 2.3rem);
  }

  .docs-hero-card::after {
    position: absolute;
    right: -1.4rem;
    bottom: -1.4rem;
    width: 3.6rem;
    height: 3.6rem;
    background: #c7dfc2;
    content: "";
  }

  .docs-hero-card .docs-card-label,
  .docs-hero-card dt {
    color: #9cca9d;
  }

  .docs-hero-card h2 {
    margin: 1rem 0 0;
    color: #f1f7ef;
    font-family: var(--font-display);
    font-size: clamp(1.8rem, 3vw, 2.6rem);
    font-weight: 500;
    letter-spacing: -0.04em;
    line-height: 0.98;
  }

  .docs-hero-card > p:not(.docs-card-label) {
    margin: 1rem 0 0;
    color: #c5d6c5;
    font-size: 0.88rem;
    line-height: 1.65;
  }

  .docs-card-rule {
    height: 1px;
    margin: 1.6rem 0 1.2rem;
    background: #476352;
  }

  .docs-hero-card dl {
    display: grid;
    gap: 0.7rem;
    margin: 0;
  }

  .docs-hero-card dl > div {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    font-family: var(--font-mono);
    font-size: 0.65rem;
  }

  .docs-hero-card dt,
  .docs-hero-card dd {
    margin: 0;
  }

  .docs-hero-card dd {
    color: #dce9dc;
    text-align: right;
  }

  .docs-layout {
    display: grid;
    grid-template-columns: 13rem minmax(0, 1fr);
    gap: clamp(2rem, 6vw, 6rem);
    width: min(100% - 2rem, 74rem);
    margin: 0 auto;
    padding: 1rem 0 7rem;
  }

  .docs-toc {
    align-self: start;
    position: sticky;
    top: 1.5rem;
    padding-top: 1.25rem;
  }

  .docs-toc-label {
    color: #708075;
    font-size: 0.62rem;
  }

  .docs-toc ol {
    display: grid;
    gap: 0.15rem;
    margin: 0.8rem 0 0;
    padding: 0;
    list-style: none;
  }

  .docs-toc a {
    display: grid;
    grid-template-columns: 1.7rem minmax(0, 1fr);
    gap: 0.35rem;
    border-left: 1px solid transparent;
    color: #627169;
    font-size: 0.77rem;
    line-height: 1.3;
    padding: 0.42rem 0.4rem;
    text-decoration: none;
  }

  .docs-toc a span {
    color: #99a89d;
    font-family: var(--font-mono);
    font-size: 0.6rem;
  }

  .docs-toc a:hover {
    border-left-color: var(--docs-green);
    background: #edf1e9;
    color: var(--docs-green);
  }

  .docs-toc-note {
    margin-top: 2rem;
    border-top: 1px solid var(--docs-line);
    padding-top: 1rem;
  }

  .docs-toc-note strong {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    text-transform: uppercase;
  }

  .docs-toc-note p {
    margin: 0.5rem 0 0;
    color: #718078;
    font-size: 0.7rem;
    line-height: 1.55;
  }

  .docs-content {
    min-width: 0;
    border-top: 1px solid var(--docs-line);
  }

  .docs-section {
    border-bottom: 1px solid var(--docs-line);
    padding: clamp(3.8rem, 7vw, 6.5rem) 0;
    scroll-margin-top: 1rem;
  }

  .docs-section-number {
    color: #518060;
  }

  .docs-section h2,
  .docs-feedback h2 {
    font-size: clamp(2.6rem, 5vw, 4.4rem);
  }

  .docs-section > p:not(.docs-section-number),
  .docs-lede,
  .docs-feedback > p:not(.docs-section-number) {
    max-width: 47rem;
    margin: 1.25rem 0 0;
    color: var(--docs-muted);
    font-size: 0.98rem;
    line-height: 1.75;
    text-wrap: pretty;
  }

  .docs-intro .docs-lede {
    max-width: 51rem;
    font-size: 1.08rem;
  }

  .docs-truth-grid,
  .docs-feature-grid,
  .docs-production-grid,
  .docs-config-grid {
    display: grid;
    gap: 1rem;
    margin-top: 2.3rem;
  }

  .docs-truth-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .docs-truth-grid article {
    min-height: 16rem;
    border: 1px solid #aebfae;
    background: #eef2eb;
    padding: 1.4rem;
  }

  .docs-truth-grid .docs-truth-planned {
    border-color: #2c5740;
    background: #183a2a;
    color: #edf6ed;
  }

  .docs-truth-planned .docs-card-label {
    color: #a7cba9;
  }

  .docs-truth-grid h3,
  .docs-config-grid h3,
  .docs-feature-grid h3,
  .docs-production-grid h3,
  .docs-role-list h3,
  .docs-two-column h3,
  .docs-side-note h3 {
    margin: 0.65rem 0 0;
    color: var(--docs-ink);
    font-family: var(--font-display);
    font-size: 1.55rem;
    font-weight: 600;
    letter-spacing: -0.035em;
    line-height: 1;
  }

  .docs-truth-planned h3 {
    color: #f1f7ef;
  }

  .docs-truth-grid article > p:not(.docs-card-label) {
    margin: 0.8rem 0 0;
    color: #5e6e64;
    font-size: 0.86rem;
    line-height: 1.6;
  }

  .docs-truth-planned article > p:not(.docs-card-label),
  .docs-truth-planned > p:not(.docs-card-label) {
    color: #c7dcc9;
  }

  .docs-truth-grid a,
  .docs-side-note a {
    display: inline-block;
    margin-top: 1.2rem;
    color: var(--docs-green);
  }

  .docs-truth-planned a {
    color: #b9dfba;
  }

  .docs-callout {
    margin-top: 2rem;
    border-left: 3px solid #60936a;
    background: #edf3ea;
    padding: 1rem 1.2rem;
  }

  .docs-callout-warning {
    border-left-color: #b07b32;
    background: #f5efe2;
  }

  .docs-callout-note {
    border-left-color: #3c7d53;
    background: #e5efe3;
  }

  .docs-callout strong {
    color: #294537;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.03em;
  }

  .docs-callout p {
    margin: 0.4rem 0 0;
    color: #5d6e63;
    font-size: 0.84rem;
    line-height: 1.65;
  }

  .docs-two-column {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: clamp(1.5rem, 4vw, 3rem);
    margin-top: 2.2rem;
  }

  .docs-prerequisites {
    align-items: start;
  }

  .docs-subheading {
    margin: 2.8rem 0 0;
    color: var(--docs-ink);
    font-family: var(--font-display);
    font-size: 1.7rem;
    font-weight: 600;
    letter-spacing: -0.04em;
  }

  .docs-check-list,
  .docs-bullet-list,
  .docs-config-grid ul {
    display: grid;
    gap: 1rem;
    margin: 1.2rem 0 0;
    padding: 0;
    list-style: none;
  }

  .docs-check-list li {
    display: grid;
    grid-template-columns: 1.1rem minmax(0, 1fr);
    column-gap: 0.65rem;
    color: #52635a;
    font-size: 0.86rem;
    line-height: 1.45;
  }

  .docs-check-list li::before {
    width: 0.75rem;
    height: 0.75rem;
    margin-top: 0.25rem;
    border: 1px solid #4c9961;
    background: #d8ebd5;
    content: "";
  }

  .docs-check-list strong {
    color: var(--docs-ink);
  }

  .docs-check-list span {
    grid-column: 2;
    color: #7a887f;
    font-size: 0.78rem;
  }

  .docs-side-note {
    border: 1px solid #b8c9b7;
    background: #e9efe6;
    padding: 1.3rem;
  }

  .docs-side-note p:not(.docs-card-label) {
    margin: 0.65rem 0 0;
    color: #5b6c61;
    font-size: 0.84rem;
    line-height: 1.6;
  }

  .docs-code {
    overflow-x: auto;
    margin: 1.25rem 0 0;
    border: 1px solid #354e42;
    background: #14221e;
    color: #dceadd;
    padding: 1.2rem 1.3rem;
    font-family: var(--font-mono);
    font-size: 0.73rem;
    line-height: 1.7;
    tab-size: 2;
  }

  .docs-caption {
    color: #718078 !important;
    font-family: var(--font-mono);
    font-size: 0.68rem !important;
    line-height: 1.7 !important;
  }

  .docs-table-wrap {
    overflow-x: auto;
    margin-top: 1.2rem;
    border-top: 1px solid #aebdaf;
  }

  .docs-table {
    width: 100%;
    min-width: 34rem;
    border-collapse: collapse;
    text-align: left;
  }

  .docs-table th {
    color: #718078;
    font-family: var(--font-mono);
    font-size: 0.64rem;
    letter-spacing: 0.08em;
    padding: 0.8rem 0.7rem;
    text-transform: uppercase;
  }

  .docs-table th:first-child,
  .docs-table td:first-child {
    padding-left: 0;
  }

  .docs-table td {
    border-top: 1px solid #d4ddd2;
    color: #5d6e63;
    font-size: 0.8rem;
    line-height: 1.5;
    padding: 0.8rem 0.7rem;
    vertical-align: top;
  }

  .docs-table td:first-child {
    color: var(--docs-ink);
    font-weight: 700;
  }

  .docs-table code {
    color: #356e49;
  }

  .docs-steps {
    display: grid;
    gap: 0;
    margin: 2.3rem 0 0;
    padding: 0;
    list-style: none;
  }

  .docs-steps li {
    display: grid;
    grid-template-columns: 2.4rem minmax(0, 1fr);
    gap: 1rem;
    border-top: 1px solid #c8d4c7;
    padding: 1.2rem 0;
  }

  .docs-steps li > span {
    display: grid;
    width: 1.8rem;
    height: 1.8rem;
    place-items: center;
    background: #d9ead6;
    color: var(--docs-green);
    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: 700;
  }

  .docs-steps strong {
    color: var(--docs-ink);
    font-family: var(--font-display);
    font-size: 1.35rem;
    font-weight: 600;
    letter-spacing: -0.025em;
  }

  .docs-steps p {
    margin: 0.35rem 0 0;
    color: var(--docs-muted);
    font-size: 0.86rem;
    line-height: 1.65;
  }

  .docs-connection-notes .docs-callout {
    margin-top: 0;
  }

  .docs-role-list {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 0.75rem;
    margin-top: 2.2rem;
  }

  .docs-role-list article {
    border-top: 3px solid #4f9160;
    background: #edf2eb;
    padding: 1rem;
  }

  .docs-role-list h3 {
    margin-top: 0;
    font-size: 1.35rem;
  }

  .docs-role-list p {
    margin: 0.6rem 0 0;
    color: #607167;
    font-size: 0.78rem;
    line-height: 1.55;
  }

  .docs-team-grid h3 {
    font-size: 1.8rem;
  }

  .docs-bullet-list li {
    position: relative;
    padding-left: 1rem;
    color: #5d6e63;
    font-size: 0.84rem;
    line-height: 1.6;
  }

  .docs-bullet-list li::before {
    position: absolute;
    top: 0.6rem;
    left: 0;
    width: 0.35rem;
    height: 0.35rem;
    background: #4f9962;
    content: "";
  }

  .docs-feature-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .docs-feature-grid article {
    border: 1px solid #c3d0c1;
    background: #f0f3ed;
    padding: 1.2rem;
  }

  .docs-feature-number,
  .docs-production-grid article > span {
    color: #528563;
    font-family: var(--font-mono);
    font-size: 0.67rem;
    font-weight: 700;
  }

  .docs-feature-grid h3 {
    margin-top: 0.9rem;
    font-size: 1.5rem;
  }

  .docs-feature-grid p {
    margin: 0.65rem 0 0;
    color: #617168;
    font-size: 0.82rem;
    line-height: 1.65;
  }

  .docs-production-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .docs-production-grid article {
    background: #e9efe6;
    padding: 1.2rem;
  }

  .docs-production-grid h3 {
    margin-top: 0.7rem;
    font-size: 1.35rem;
  }

  .docs-production-grid p {
    margin: 0.65rem 0 0;
    color: #617168;
    font-size: 0.8rem;
    line-height: 1.6;
  }

  .docs-method {
    display: inline-block;
    min-width: 3.1rem;
    padding: 0.25rem 0.35rem;
    text-align: center;
    font-family: var(--font-mono);
    font-size: 0.58rem;
    font-weight: 700;
  }

  .docs-method-get { background: #dbeedb; color: #2f7148; }
  .docs-method-post { background: #f6e8d1; color: #8c5d22; }

  .docs-api-table td:nth-child(2) {
    white-space: nowrap;
  }

  .docs-faq-list {
    margin-top: 2.3rem;
    border-top: 1px solid #aebdaf;
  }

  .docs-faq details {
    border-bottom: 1px solid #c8d3c9;
    padding: 1.15rem 0;
  }

  .docs-faq summary {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    cursor: pointer;
    color: #20372b;
    font-family: var(--font-display);
    font-size: 1.3rem;
    font-weight: 600;
    list-style: none;
  }

  .docs-faq summary::-webkit-details-marker {
    display: none;
  }

  .docs-faq summary::after {
    color: #2d7748;
    content: "+";
    font-family: var(--font-mono);
    font-size: 1rem;
  }

  .docs-faq details[open] summary::after {
    content: "−";
  }

  .docs-faq details p {
    max-width: 44rem;
    margin: 0.75rem 2rem 0 0;
    color: #617168;
    font-size: 0.86rem;
    line-height: 1.65;
  }

  .docs-faq a,
  .docs-content code {
    color: #316e48;
  }

  .docs-content code {
    font-family: var(--font-mono);
    font-size: 0.86em;
  }

  .docs-code code {
    color: inherit;
    font-size: inherit;
  }

  .docs-feedback {
    padding: clamp(3.8rem, 7vw, 6rem) 0 0;
  }

  .docs-feedback h2 {
    font-size: clamp(2.6rem, 5vw, 4rem);
  }

  @media (max-width: 900px) {
    .docs-hero {
      grid-template-columns: 1fr;
    }

    .docs-hero-card {
      max-width: 34rem;
    }

    .docs-layout {
      grid-template-columns: 1fr;
    }

    .docs-toc {
      position: static;
      border-top: 1px solid var(--docs-line);
      padding-top: 1rem;
    }

    .docs-toc ol {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 0.2rem;
    }

    .docs-toc-note {
      display: none;
    }
  }

  @media (max-width: 680px) {
    .docs-hero,
    .docs-layout {
      width: min(100% - 1.5rem, 74rem);
    }

    .docs-hero {
      padding-top: 3.5rem;
    }

    .docs-hero h1 {
      font-size: clamp(3rem, 15vw, 4.6rem);
    }

    .docs-toc ol,
    .docs-truth-grid,
    .docs-two-column,
    .docs-feature-grid,
    .docs-production-grid,
    .docs-config-grid,
    .docs-role-list {
      grid-template-columns: 1fr;
    }

    .docs-toc ol {
      gap: 0;
    }

    .docs-toc a {
      padding-block: 0.32rem;
    }

    .docs-truth-grid article {
      min-height: 0;
    }

    .docs-table {
      min-width: 31rem;
    }

    .docs-faq summary {
      font-size: 1.15rem;
    }
  }
</style>
